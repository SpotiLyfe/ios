import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }
    homeButton: {
        width: 100vw,
        height: 100vw,
        borderRadius: 100/2,
        backgroundColor:'red'
    }
});
